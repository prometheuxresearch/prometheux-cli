# Market segments

A pristine body file: plain content, no frontmatter, safe to keep in your own docs repo.
A context-set manifest (`*.context.md`) points at this file; the manifest — not this
file — carries scope and activation. Edit this text freely; on the next apply the note
is re-embedded from it.
